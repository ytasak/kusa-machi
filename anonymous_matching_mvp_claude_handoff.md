# 匿名マッチング MVP — 実装引き継ぎ仕様書

## 0. ゴール

`kusa` SNS の iframe 内で動き、かつ自分の URL から直接開いても使える、軽量な
匿名マッチングシミュレーションゲームを作る。

中心となるコンセプト:

- 毎日、各参加者にランダム生成された架空の Persona が1つ配られる。
- Persona の「ステータス」属性はその日のあいだ固定される。
- 参加者が編集できるのは、軽い自己表現の項目だけ。
- 参加者は他人の Persona を閲覧し、1日分の限られた Like を使う。
- 相互 Like で Match が成立する。
- ゲームデータはすべて一時的で、毎日 00:00 JST にリセットされる。
- 履歴・ランキング・チャット・永続プロフィールは存在しない。

主な体験:
1. `マッチング市場のシミュレーション`
2. 入口の体験としての日替わり Persona ガチャ
3. ロールプレイは任意で、ユーザーに委ねる

これは個人の趣味の MVP。拡張性や作り込みよりも、単純で明示的な実装を優先する。

---

# 1. 技術スタック

## バックエンド
- Go
- HTTP ルータ: `chi`
- PostgreSQL
- SQL アクセス: `sqlc`
- マイグレーション: `golang-migrate`

## フロントエンド
- Svelte + Vite
- CSS Modules

## デプロイ
- 1コンテナ構成でよい
- フロントエンドと API は同一 Origin で配信する
- 次のどちらでも動くこと:
  - kusa の iframe 内
  - URL を直接開いた場合

---

# 2. プロダクトの原則

## 2.1 日次の一時性

ゲームデータはすべて1つの暦日にスコープされる。

00:00 JST に:
- その日の Persona が失効する
- Like が失効する
- Pass が失効する
- Match が失効する
- 参加者が入力した 名前 / 趣味 / ひとこと が失効する
- 前日のゲームデータは即座にアクセス不能になる
- 古いデータは非同期に物理削除される

存在しないもの:
- 履歴
- 累積統計
- 連続記録
- ランキング
- 過去の Persona
- 過去の Match
- 過去の Like 数

## 2.2 アカウントシステムなし

kusa は iframe 内のアプリにユーザー identity を提供しない。

ブラウザは匿名 Cookie で識別する。

Cookie を消せば新しい参加者 identity を得られてしまうが、MVP ではそれを許容する。
再抽選対策は不要。

---

# 3. ユーザー identity のモデル

3つの概念を明確に分離し続けること。

## `cookie_token`
ブラウザの識別子。

- 不透明なランダム ID
- UUID v4 かそれ相当
- Cookie にのみ保存する
- 有効期限は約30日
- ゲームの状態は Cookie に保存しない

Cookie の属性:
- `HttpOnly`
- `Secure`
- `SameSite=None`
- `Partitioned`
- `Path=/`
- `Domain` は明示しない

> **改訂メモ（実装時に追加）**
> `Partitioned` は当初の仕様には無かったが、kusa の iframe から見ると
> このアプリはサードパーティであり、`SameSite=None` だけではサードパーティ
> Cookie を遮断するブラウザ（古い iOS Safari など）で Cookie が保存されない。
> 保存されないとリクエストのたびに別の Participant が作られ、CSRF トークンが
> 一致せず何も操作できなくなる。`Partitioned` を付けると埋め込み元サイトごとに
> 分離された領域へ保存され、遮断下でも identity が保てる。
> なお `Partitioned` に未対応のブラウザでは依然として Cookie が保存されない。
> その場合は §16 の「遊べません」画面へ誘導する。

## `Participant`
1ゲーム日ぶんの技術的な参加者。

関係:

```text
cookie_token -> Participant -> Persona
```

`cookie_token + game_date` につき Participant は1つ。

Participant 自体はゲーム内には現れない。

## `Persona`
マッチング市場で使われる架空の人物。

ゲーム内の操作はすべて `persona_id` を使い、Participant や Cookie の identity は使わない。

Like / Pass / Match の主体は Persona。

---

# 4. 1日のライフサイクル

タイムゾーン: **Asia/Tokyo**

## その日の初回アクセス

1. 匿名 Cookie を読むか発行する。
2. その日の Participant の存在を保証する。
3. その日の Persona が存在しない場合:
   - 「新しい人生を始める」を表示する
4. 押されたら:
   - サーバが Persona を一度だけ生成する
   - 即座に保存する
   - Persona は即座に公開され、市場に出る
5. 1〜2秒の短い抽選中の溜めを見せる。
6. 生成された属性を1つずつ開示する（§7.7 / §16）。

> **改訂メモ（実装時に追加）**
> 当初は「属性を一度にすべて開示する」としていたが、引く体験を作るために
> 1属性ずつの開示に変えた。開示し終えたあとに全属性の揃ったカードを出すので、
> 最終的に見える情報は変わらない。

Persona 生成 API は冪等であること:
- その日の Persona が無い → 生成する
- すでに生成済み → 既存の Persona を返す
- 同じ日のうちに振り直しは絶対にしない

## 1日の終わり

常時、次のようなカウントダウンを表示する:

```text
今日の人生 残り 03:42:18
```

23:55 に、アプリが開かれていれば、アプリ内モーダルを表示する:

```text
今日の人生はあと5分です
```

00:00 に:
- その日は即座に無効になる
- 開いているクライアントには次を表示する:
  - `今日の人生が終了しました`
- 翌日の Persona を作るには、ユーザーが `新しい人生を始める` を押す必要がある

正しさを 00:00 の削除ジョブに依存させないこと。
すべてのクエリが `game_date = 当日(JST)` でデータをスコープすること。

---

# 5. Persona のモデル

## 5.1 システム生成の不変属性（A）

1日に一度だけ生成され、以後不変:

- 年齢
- 性別
- 身長
- 学歴
- 職業
- 年収

## 5.2 ユーザーが編集できる属性（B）

その日のあいだ何度でも編集できる:

- 名前
- 趣味
- ひとこと
- プロフィール写真 （2026-08-16 追加。§23 の Non-Goal から外した）

制限:
- 名前: 最大20文字
- 趣味: 最大30文字
- ひとこと: 最大60文字

ルール:
- 単一行のみ
- 改行は不可
- 前後の空白は除去する
- 空白のみの場合は未設定（null）にする
- プレーンテキストのみ
- Markdown は解釈しない
- HTML として描画しない
- 出力時はエスケープする
- 明示的な URL（`http://`、`https://`）は禁止
- 電話番号・メールアドレス・SNS ID など、外部連絡先のあらゆる表記を検出しようとはしない
- MVP では NG ワードフィルタを持たない

名前 / 趣味 / ひとこと が未設定の場合、その項目は Persona カードから丸ごと省く。

## 5.3 プロフィール写真

任意。未設定のときはシンプルな既定のシルエットを表示する。

クライアント側:
- アップロード前に中央で正方形に切り抜き、長辺1024px以下に縮小する
- これはアップロードを小さくするためだけのもので、セキュリティ制御ではない

サーバ側（信用してよい唯一の場所）:
- JPEG と PNG のみ受理し、それ以外は拒否する
- 8MB を超えるボディと4000万画素を超える画像を拒否する。画素数チェックは
  画像をデコードする前にヘッダに対して行う
- 必ずデコードして JPEG として再エンコードする。これにより EXIF が消える
  （匿名アプリが誰かの GPS 座標を公開してはならない）とともに、画素に紛れ込ませた
  ものも捨てられる
- 長辺1024pxで保存する

保存と寿命:
- `PHOTO_DIR` 配下のファイルとして保存し、`game_date` ごとにディレクトリを分ける
- 日次クリーンアップが今日より古いディレクトリを削除するため、写真もその日の
  他のデータと一緒に失効する
- 配信時は `Content-Type: image/jpeg` と `X-Content-Type-Options: nosniff` を付け、
  当日の Persona のものだけを配信する
- 参加者はいつでも自分の写真を削除できる

この MVP で受け入れている既知の制約: 通報の導線もモデレーションも無いため、
アップロードされた写真は本人が削除するか日次リセットが来るまで残る。

---

# 6. Persona カードの表示順

0. プロフィール写真（未設定なら既定のシルエット）
1. 名前（設定されている場合のみ）
2. 年齢 + 性別
3. 身長
4. 職業
5. 年収
6. 学歴
7. 趣味（設定されている場合のみ）
8. ひとこと（設定されている場合のみ）

公開 Persona カードに表示してはならないもの:
- Like 数
- Match 数
- 人気ランク
- レア度
- 総合スコア
- 露出回数
- Participant ID
- Cookie 識別子

> **改訂メモ（実装時に追加）**
> §7.7 のレア度は、自分の Persona を引いた直後の開示演出の中でだけ出す。
> Persona カードには出さないので、この禁止事項はそのまま維持されている。
> 他人のレア度が見える経路は無い。

Persona カードの UI コンポーネントは次の画面で共通のものを使う:
- 探す
- Likeされた
- 送信済みLike
- Match

画面ごとに変えてよいのは、アクションとバッジだけ。

---

# 7. Persona の生成

生成順:

```text
年齢
 -> 性別
 -> 身長
 -> 学歴
 -> 職業
 -> 年収
```

目指すのは**最低限の妥当性の制約**であって、現実的な人口統計のシミュレーションではない。

## 7.1 年齢

範囲: 20〜50

重み付きの年齢帯:

| 範囲 | 重み |
|---|---:|
| 20–24 | 20% |
| 25–29 | 25% |
| 30–34 | 20% |
| 35–39 | 15% |
| 40–44 | 10% |
| 45–50 | 10% |

手順:
1. 重みに従って帯を選ぶ
2. その帯の中から一様に年齢を選ぶ

## 7.2 性別

- male: 50%
- female: 50%

MVP では性別によるフィルタリングを行わない。

## 7.3 身長

一様乱数:

```text
140–200 cm
```

1cm 刻み。

性別による補正はしない。

## 7.4 学歴

| 学歴 | 重み |
|---|---:|
| 中卒 | 5% |
| 高卒 | 20% |
| 専門卒 | 15% |
| 短大卒 | 10% |
| 大卒 | 35% |
| 大学院卒 | 10% |
| ホイ卒 | 5% |

年齢による制約:
- 大卒: 22歳以上
- 大学院卒: 24歳以上
- その他: 20歳以上

不適格な候補は除外し、重みを再正規化する。

### 特例: ホイ卒
`ホイ卒` は意図的な kusa の内輪ネタであり、レアなミーム学歴。

学歴が `ホイ卒` の場合、職業の制約はすべて無視する。

## 7.5 職業

| 職業 | 重み |
|---|---:|
| 公務員 | 7% |
| 医師 | 2% |
| 看護師 | 6% |
| 教員 | 6% |
| ITエンジニア | 10% |
| 営業 | 10% |
| 事務 | 10% |
| 販売・接客 | 10% |
| 飲食 | 8% |
| 建設 | 8% |
| クリエイター | 6% |
| 自営業 | 6% |
| 経営者 | 2% |
| フリーター | 5% |
| 無職 | 4% |

制約:
- 医師:
  - 24歳以上
  - 学歴が 大卒 か 大学院卒
- 教員:
  - 22歳以上
  - 学歴が 大卒 か 大学院卒
- 経営者:
  - 25歳以上
- 看護師:
  - 追加の制約なし
- その他:
  - 追加の制約なし
- 学歴が `ホイ卒` の場合:
  - 職業の制約をすべて無視する

不適格な候補は除外し、重みを再正規化する。

## 7.6 年収

保存単位: **万円**

値はすべて10万円刻みであること。

| 職業 | レンジ（万円） |
|---|---:|
| 公務員 | 300–750 |
| 医師 | 700–1800 |
| 看護師 | 300–750 |
| 教員 | 300–750 |
| ITエンジニア | 300–900 |
| 営業 | 300–900 |
| 事務 | 250–550 |
| 販売・接客 | 250–550 |
| 飲食 | 250–550 |
| 建設 | 300–900 |
| クリエイター | 200–1200 |
| 自営業 | 200–1200 |
| 経営者 | 300–3000 |
| フリーター | 100–300 |
| 無職 | 0–100 |

年齢による補正は意図的に弱くする:
- 20代: やや低め〜中程度に寄せる
- 30代: ほぼ平坦
- 40〜50代: やや中程度〜高めに寄せる

極端な組み合わせも起こりうる状態を保つこと。

ここは作り込まない。単純に歪ませた乱数で十分。

レア度や「良い人生／悪い人生」のスコアは一切計算しない。

> **改訂メモ（実装時に追加）**
> この「レア度を計算しない」は**生成と保存の話**として維持する。生成器は
> 従来どおりレア度を一切扱わず、DB にもレア度に類する列は持たない。
> ただし開示演出（§7.7）では、確定済みの値を**表示のためだけに**レア度へ
> 読み替える。抽選・保存・マッチングのいずれにも影響しない。

## 7.7 開示演出のレア度（表示のみ）

新しい人生を始めたとき、A属性を1つずつ開示するガチャ演出を出す（§16）。
その中でだけ使う、**表示専用**のレア度を定義する。

原則:

- レア度は**生成された値を後から解釈し直したもの**であり、抽選には一切関与しない
- DB に保存しない。API で返さない。Persona カードにも出さない
- マッチング、Like 予算、探すの候補選定に影響しない
- しきい値がずれても壊れるのは演出の派手さだけ。仕様上の正しさには関わらない

**開示順**は §7 の生成順と同じ:

```text
年齢 -> 性別 -> 身長 -> 学歴 -> 職業 -> 年収
```

年収を最後に置くのは、職業レンジのぶん振れ幅がいちばん大きく、山場になるため。

### レア度の3段階

| 段階 | スコア | 表示 |
|---|---:|---|
| 通常 | 0 | 何も出さない |
| レア | 1 | 金色の枠と「✦ レア ✦」 |
| 激レア | 2 | 虹色の枠と「✦ 激レア ✦」＋粒が弾ける |

### 属性ごとの判定

上から順に評価し、最初に当たったものを採用する。

| 属性 | 条件 | 段階 | 添える語 |
|---|---|---|---|
| 年齢 | `<= 20` | レア | 最年少 |
| 年齢 | `>= 50` | レア | 最年長 |
| 性別 | — | 常に通常 | — |
| 身長 | `>= 198` または `<= 142` | 激レア | 規格外 |
| 身長 | `>= 190` | レア | 高身長 |
| 身長 | `<= 150` | レア | 小柄 |
| 学歴 | ホイ卒 | 激レア | 伝説の学歴 |
| 学歴 | 大学院卒 | レア | 高学歴 |
| 学歴 | 中卒 | レア | 叩き上げ |
| 職業 | 医師 | 激レア | エリート |
| 職業 | 経営者 | 激レア | 成功者 |
| 職業 | 無職 | 激レア | ネタ枠 |
| 職業 | フリーター | レア | 自由人 |
| 年収 | `>= 1000` | 激レア | 大台突破 |
| 年収 | `>= 700` | レア | 高収入 |
| 年収 | `<= 100` | レア | 崖っぷち |

性別は 50/50 で当たり外れが作れないため、常に通常とする。テンポを作るための
短い一拍として残している。

しきい値は §7 の重みから逆算している。ホイ卒 5%、医師・経営者 各2% のように、
もともと出にくい値を激レアに割り当てている。

### 総合レア度

各属性の段階を合計し、最後にランクを出す。

| スコア | ランク | 文言 |
|---:|---|---|
| 5 以上 | SSR | 伝説の人生を引き当てました |
| 3–4 | SR | なかなかの人生です |
| 1–2 | R | 少し変わった人生です |
| 0 | N | ごく普通の人生です |

スコアの期待値はおよそ 1。たいていは N か R に落ち、SR 以上はたまにしか出ない。
この配分を保つこと。全員が SSR になる調整はしない。

---

# 8. マッチングのルール

## 1日の Like 予算

各 Persona がその日に持つ Like はちょうど:

```text
10 Like / 日
```

ルール:
- Pass は無制限
- Like は即座に消費される
- Like は取り消せない
- 使わなかった Like は繰り越されない
- Like 予算は新しい日にリセットされる
- Like の上限はサーバ側のトランザクションで強制する
- 同じリクエストが重複しても Like を二重に消費しない
- 同じ相手に Like できるのは1日1回まで
- 自分への Like は禁止

Likeされた相手へのお返しも、同じ10 Likeの予算を共有する。

返信用の別枠は存在しない。

## Like の回復

ゲーム内の行動で少量だけ Like が戻る。Like を希少なままにしておきたいので、
回復はごく小さく、回数で天井を作る。

| 行動 | 回復量 | 上限 |
| --- | --- | --- |
| 名前・趣味・一言をすべて設定する | +1 | 1日1回 |
| Match が成立する | +2 | 1日2回まで |
| Like を使ってから3時間経過する | +1 | 回数の上限なし |

所持数の上限:

```text
12 Like
```

現在の所持数が上限に達している状態で回復が起きても、溢れた分は失われる。
後から復活することはない。

したがって、その日に使える Like は最大で:

```text
10 + 1 + 2 × 2 + 3時間ごとの回復
```

時間回復に回数の上限は無いが、0:00 のリセットまでという時間の天井がある。
初 Like が 0:00 直後なら最大7回、昼に使い始めたなら3〜4回といったところ。

所持上限が 12 なので、これらを同時に持つことはできない。満額まで回復を
受け取るには、使いながら回復する必要がある。Match の成立にも Like の消費が
必要で、時間回復には時間の経過が必要なので、上の合計に届くのは1日を通して
使い続けた日だけになる。

ルール:
- 回復量・回数・所持上限はすべてサーバ側で強制する
- プロフィール完成報酬は、一度受け取った後に項目を消して入れ直しても再発しない
- 同じ PATCH の再送やページ再読み込みでも二重に付与しない
- 同一の Match に対して報酬を複数回付与しない
- Match 報酬はペアの両方に払う。先に Like して待っていた側も受け取る
- 回復の状態も 0:00 のリセットで初期化される。前日の状態は引き継がない

### 時間回復

Like を使ったあと、時間の経過でも少しだけ戻る。

```text
3時間ごとに +1、回数の上限なし、0:00 のリセットまで
```

**タイマーは初めて Like を1つ消費した時点から動き出す。** 初期の10を持っている
だけの状態では進まない。

```text
10:00  Persona 生成          → タイマーは動かない
13:10  1つ目の Like を消費    → ここが起点になる
16:10  +1
19:10  +1
22:10  +1
0:00                   → 日次リセット
```

こうするのは2つの理由から:
- 初期の10に時間回復が自動で上積みされていく設計を避ける
- Like を使った人にだけ、再訪する理由を作る

3時間ごとに見に来る必要はない。アプリを閉じている時間も回復時間として経過し、
次に開いた時点で経過した3時間単位をまとめて計算する。

```text
09:00  初 Like → 9 / 12、以降アクセスなし
18:30  再訪 → 9時間30分の経過で floor(9.5h / 3h) = 3 回ぶん成立 → 12 / 12
```

起点は経過した3時間単位ぶんだけ進む。上の例なら 09:00 → 18:00 になり、余った
30分は次回の計算へ持ち越される。2つ目以降の Like で起点が今に進むことはない。
使うほど回復が遠のいてしまうため。

**所持上限を超える回復は失われる。** 上の例で残数が 11 だった場合、3回ぶんが
成立していても増えるのは1つで、残り2回ぶんは消える。後から Like を消費しても
戻ってこない。

```text
09:00  11 / 12
18:30  3回ぶん成立 → 12 / 12（実際の回復は +1、残り2回ぶんは失われる）
```

未取得の回復回数をストックする仕組みは持たない。12 / 12 のまま6時間が経っても
「内部的に2つ貯まっている」という状態にはならない。その6時間は起点を進めるだけで
消費され、Like を1つ使った後は通常の間隔から回復し直す。スタミナシステムを
単純に保つため、意味論を満たす最も単純な方式（起点を経過ぶんすべて進める）を
採る。

回復の判定はサーバ側で行い、Cron やバックグラウンドジョブは持たない。残数を返す
エンドポイント（`GET /api/home`、`GET /api/discover`、`POST /api/likes`、
`PATCH /api/persona/profile`）で、そのときの現在時刻から計算して反映する。
毎分 DB を更新するような仕組みは要らない。

新しい通貨やポイントは追加しない。回復するのは Like そのものだけ。

回復は**もらう前から見えていること**。完成させて初めて分かる作りでは、
プロフィールを埋める理由が伝わらない。したがって、まだ受け取れる報酬は
獲得前から画面に出す（§14 / §15 / §10 を参照）。

受け取り済みの報酬は誘導しない。API が `profile_reward_available` を返すので、
画面はこれを見て訴求を出し入れする。日次リセットでまた true に戻る。

回復した Like は画面上で必ず知らせる。文言と出す場所は §14 / §15 を参照。
所持上限で実際の増分が減った場合は、増えた数だけを出す（`11 → 12` なら
`Like +1`）。まったく増えなかった場合は回復の表示を省く。

> **改訂メモ（実装時に追加）**
> もともと Like は1日10で固定だった。「プロフィールを整える」「Match を
> 成立させる」に対してゲーム上の報酬が何も無く、プロフィールを埋める動機が
> 弱かったため、少量の回復を後から足した。
>
> 希少性を壊さないことを優先し、次の3つで天井を作っている:
> 回数の上限（プロフィール1回 / Match 2回）、所持上限 12、そして溢れた分を
> 失わせること。Match 報酬を回数で打ち止めにしているのは、Match が増えるほど
> Like が増えるという正のフィードバックを避けるため。
>
> 予算の判定は「送信済み < 10」ではなく「残数 > 0」で行うこと。前者のままだと
> 回復した Like を1つも使えないまま日が終わる。

> **改訂メモ（時間回復の追加時）**
> Like が尽きた時点でその日にできることが無くなり、再訪する理由が消えていた。
> プロフィール完成と Match はどちらも「もう一度来る理由」にはならない（前者は
> 1日1回で終わり、後者は Like が無ければ起こせない）ため、時間経過による
> 回復を補助として足した。
>
> 希少性を壊さないよう、天井は3時間という間隔と所持上限 12 の2つで作る。
> ソシャゲ的なスタミナシステムには発展させない。
>
> 回数の天井は当初1日2回にしていたが、「Like を使った人が数時間後にまた
> 遊べる」という再訪ループを素直にするため撤廃した。あわせて、上限に達して
> いる間の経過時間を取り置くのもやめた。取り置くと満タン中の時間が後から
> まとめて回復に変わり、回復ストックを持つのと変わらなくなる。経過した3時間は
> 常に起点を進めて消費し、残高に入らなかった分は失わせる。
>
> あわせて残数の持ち方を変えた。もともとは `10 + bonus_likes - 送信済み Like`
> で導出していたが、時間回復は「Like を送っていないのに残数が増える」ため
> 送信数からの引き算では表せない。`like_balance` をサーバ側の明示的な状態に
> 改め、`bonus_likes` は捨てた（§18 を参照）。同じ意味の状態を2つ持たない。

## Match

相互 Like で Match が1つ成立する。

Match:
- 順序を持たない
- Persona のペアにつき1つ
- 冪等であること
- 都度計算するのではなく明示的に保存する

相互 Like が成立したとき:
- Like API は `matched = true` を返す
- フロントエンドは Match アニメーションを表示する
- Match が Match 一覧に現れる

MVP にチャットや DM は無い。

---

# 9. Pass のルール

Pass は除外ではない。

一度 Pass した Persona もあとでまた表示される。

ルール:
- Persona ペアの方向ごとに `pass_count` を保持する
- 何回 Pass しても、その相手はあとでまた出てよい
- Pass の回数で候補から外すことはしない
- 自分への Pass は禁止
- 相手が Like 済み / Match 済みになったら、もう Pass の操作はできない

かつては3回目の Pass でその日はもう出さない仕様だったが廃止した。ユーザー数が
少ないうちは探すに出せる相手がそもそも少なく、Pass で候補を削ると出す相手が
枯渇するため。`pass_count` は記録として残すが、除外の判断には使わない。

クールダウン:
- Pass した直後の Persona は、次に表示・評価される5枚のあいだは再表示しない
- MVP ではこの5枚のクールダウンをフロントエンドのセッション状態で保持してよい
- ページを再読み込みするとクールダウンが失われるが、それは許容する
- 再表示を遅らせる仕組みはこのクールダウンだけで、サーバ側には無い

---

# 10. 探す / 市場での露出

探す画面の UI:
- 一度に1枚の Persona カード
- Like ボタン
- Pass ボタン
- MVP にスワイプ操作は無い
- 確認ダイアログは無い
- Pass は素早く次へ遷移する
- Like には短い `LIKE` のフィードバックアニメーションを付ける
- Match したときは直接 Match アニメーションへ遷移する

常に表示するもの:
- 残り Like `N / 12`（ヘッダーに出る。§14 を参照）
- Likeされた件数の小さなバッジ

次の時間回復までの残り時間はカードの下ではなくヘッダーに出す（§14 を参照）。
残数と「それが次にいつ増えるか」は同じことを言っているので、離して置くと
結び付かない。

Like を使い切ったときは、Like ボタンの下に回復の案内を出す。ここが
「どうすれば増えるのか」を最も知りたい瞬間。

- プロフィール完成報酬が未取得なら、それを出す。押すと編集画面へ移動する
- 受け取り済みなら Match だけを案内する（押せる導線は無い）

探す画面に Match 数は表示しない。

## 候補の選び方

`GET /api/discover` は一度に最大5件の Persona を返す。

選択の条件:
- 自分ではない
- 当日の `game_date`
- Persona が存在し有効である
- リクエスト元がまだ Like していない
- まだ Match していない
- Pass 済みでも候補に含める（回数によらない）
- フロントエンドから除外 ID が渡された場合はそれを尊重する
- 1回のレスポンス内に同じ Persona を含めない
- 前回のバッチと重複するのは構わない

優先度:
1. `exposure_count` が少ない順
2. 同程度の露出の中ではランダム順

## 露出のカウント

探す API がバッチを返した時点では露出を加算しない。

次のいずれかをユーザーが確定したときにのみ:

```text
exposure_count += 1
```

- Like
- または Pass

つまり露出は「実際に評価されたプロフィール」を表す。

フロントエンドは現在のバッチが尽きかけたら自動的に次のバッチを先読みする。

バッチの切れ目がユーザーから見えてはならない。

---

# 11. Likeされた

画面:
- 現在の Persona に Like した Persona を一覧表示する
- 新しい順
- 公開 Persona カードをそのまま表示する
- Like の時刻は表示しない
- 送信者の Like 予算の情報は表示しない

ユーザーは Like を返せる。

Like を返す操作:
- 同じ1日10 Likeの予算を消費する
- 相互 Like になれば Match が成立する

マイページ:
- Likeされた件数を表示する
- 未読の Like があれば目立つ形で表示する:
  - `新しいLikeがあります`
- Likeされた画面を開いたらバッジを消す

リアルタイム通信は不要。
状態は画面遷移やリクエストのタイミングで更新される。

---

# 12. 送信済みLike

画面:
- 現在の Persona が Like した Persona を一覧表示する
- 新しい順
- Like は取り消せない
- Match した相手も一覧に残る
- Match した相手には `MATCH` バッジを付ける

この画面はその日の Like 配分の履歴として機能する。

データはすべて 00:00 に消える。

---

# 13. Match

画面:
- Match した相手の Persona を一覧表示する
- 新しい順
- 相手の Persona カードのみ表示する
- 自分の Persona は繰り返し表示しない
- 読み取り専用

マイページ:
- その日の Match 数を表示する
- 未読の Match があれば目立つ形で表示する:
  - `新しいMatchがあります！`
- Match 画面を開いたらバッジを消す

Match 成立時のアニメーション:
- 自分の Persona と相手の Persona を並べて表示する
- 短く `MATCH!` を出す
- コピーには次を含めてよい:
  - `今日の人生でマッチしました`

チャットや DM は無い。

---

# 14. ナビゲーションとマイページ

> **改訂。** この節はもともと「ホームがナビゲーションの起点。常時表示の
> ボトムタブは持たない」だった。2026-08-16 にこれを取り消した。ここで唯一
> 想定している端末であるスマートフォンでは、常時表示のタブバーのほうが
> 片手で届きやすいため。ホーム画面はマイページに置き換わり、カウントダウンは
> 常時表示のヘッダーへ移してすべての画面で見えるようにする。

## 常時表示の要素

その日の Persona が存在するようになると、すべての画面でヘッダーを表示する:

1. 日次カウントダウン
2. 残り Like `N / 12` と、次の時間回復までの時間

分母は1日の予算 10 ではなく所持上限の 12（§8 を参照）。予算を分母にすると、
回復して 11 や 12 になったときに「11 / 10」と表示されてしまう。分母は API の
`like_capacity` から受け取る。上限を決めるのはサーバなので、フロントに同じ
定数を置かない。

残 Like の表示は回復した直後に更新する。ホーム・探す・マイページのどこを
見ていても、サーバが返した残数がそのまま出ること。

次の時間回復までの時間は、残数と同じ丸の中に並べて出す（§8 を参照）:

```text
❤️ 9 / 12 | Like回復まで 01:57
```

残数と離して置くと、この2つが同じことを言っていると分からない。文言は
「回復までの待ち時間」であって「Like を送れない時間」ではないので、
`次のLikeまで` のような言い方は避ける。3時間 Like できないと読める。

刻みは分まで。ヘッダーには秒まで動く日次カウントダウンが既にあり、そこに
秒を持つ数字を2つ並べるとどちらを読むのか分からなくなる。分は切り上げる
（残り30秒を `00:00` とは出さない）。320px 級の画面ではラベルを時計アイコンに
置き換え、残数と時刻だけを1行に残す。

出す条件は「サーバが `next_recovery_at` を返しているとき」だけでよい。次の
3つはサーバ側で null になるので、画面はその判定を持たない:
- まだ Like を1つも使っておらず、タイマーが動いていない
- 所持数が上限に達している
- 次の回復が 0:00 より後になる（日次リセットで満了しないタイマー）

いずれの場合も時間は出さず、残数だけを出す。満タンは `12 / 12` がそのまま
示している。

```text
❤️ 12 / 12
```

回復が起きたことは、残数が1ポイント上がる様子そのもので知らせる。数字が
跳ねる短いアニメーションを1回だけ添える。まとめて回復した日は、回復した数だけ
1ポイントずつ続けて上がるので、増えた数がそのまま見える。所持上限で1つも
増えなかった場合は、上がる数字が無いので何も起きない。派手な演出は要らない。

アプリを開いた時点ですでに回復が反映されていた場合も、回復前の数から
上げていく。「戻ってきたら増えていた」がこの機能の主役なので、その瞬間だけ
演出が無いのはおかしい。API の `likes_recovered` がこの起点に使える。

カウントダウンは表示用であり、回復判定の正は必ずサーバ。クライアントのタイマー
だけで残数を増やさないこと。表示はサーバが返した `next_recovery_at` と
`server_time` を基準にするので、ページを再読み込みしても状態が復元できる。

カウントダウンが 0 になったら、ホームを一度だけ読み直して残数を反映する。
3時間に1回の往復なので、ポーリングにはしないこと。

その日の Persona が存在するようになると、すべての画面でボトムタブバーを表示する。

「探す」はこのアプリの主行動なので、**タブの1つとして端に並べず、下部ナビの
中央に独立した1つのボタンとして置く**。左右に従属のタブが2つずつ並ぶ。

```text
 送信済み  Likeされた   ( 探す )   Match  マイページ
                          ↑
                      画面中央ちょうど
```

主従は次のように付ける。両者を同じ強さで描かないこと。

| | 探す | 左右のタブ |
|---|---|---|
| 位置 | 中央。バーの上へせり出す | バーの中に収まる |
| 大きさ | 56px の円 | アイコン 22px |
| 色 | コーラルのグラデーション | ニュートラルなグレー |
| 影 | 浮いて見える影を落とす | なし |
| 選択中 | 常に目立つ。外側にリングが増える | 濃さだけで示す。アクセント色は使わない |

未読の Like と未読の Match は、それぞれのタブにドットを出す。

その日の Persona が存在しないあいだは「新しい人生を始める」画面がアプリ全体を
占有する。ヘッダーもタブバーも出さない。

> **改訂メモ（実装時に追加）**
> もともとタブは 探す / Likeされた / Match / マイページ の4つで、探すは
> 左端の1タブだった。主行動が端に埋もれていたため、探すを中央の主 CTA に
> 引き上げた。
>
> 4項目のままでは中央に真ん中の位置が無い（4分割の中心は項目の境目）ため、
> マイページ配下にあった **送信済みLike をタブへ昇格**し、左右2つずつの
> 対称にした。これに伴い送信済みLike 画面はマイページへ戻る導線を持たず、
> マイページのメニューからも外している。
>
> タブの並びは中央に近いほど優先度が高い。探すのすぐ隣に Likeされた と
> Match を置き、送信済み と マイページ を外側に置く。
>
> 探すはバーの上へせり出すので、せり出したぶんの余白をどの画面でも確保する
> こと（`--nav-lift`）。とくに探す画面は Like / Pass の丸ボタンがバーの
> すぐ上にあり、間隔を詰めると中央のボタンと1組に見えてしまう。

## マイページ

主要な情報:
1. その日の自分の Persona カード
2. 残り Like `N / 12`
3. Likeされた件数
4. その日の Match 数

プロフィール完成報酬が未取得なら、残り Like のすぐ下に訴求を1行出す。
数字と並べることで「何をすれば増えるのか」が同時に読める。

```text
プロフィールを完成させると Like +1  →
```

押すとプロフィール編集へ移動する。未読の Like / Match の知らせ（`.notice`）と
同じ塗りにしないこと。あちらは状態の通知、こちらは押させたい誘導なので、
並んだときに区別がつかなくなる。白地に枠と矢印を付けて押せるものとして見せる。

マイページから遷移し、マイページへ戻る導線を持つ画面:

- プロフィール編集

（送信済みLike はタブへ昇格したため、ここには含めない。上の改訂メモを参照。）

---

# 15. プロフィール編集画面

上部:
- 不変の A属性を読み取り専用で表示する

その下で編集できるもの:
- 名前
- 趣味
- ひとこと
- プロフィール写真

名前・趣味・ひとことの3つがそろった保存では Like が1回復する（§8 を参照）。

報酬が未取得のあいだは、**入力欄の手前に条件を出す**:

```text
名前・趣味・ひとことをすべて埋めると Like +1（1日1回）
```

その保存に限り、通常の「保存しました」の代わりに軽いフィードバックを出す:

```text
プロフィール完成！ Like +1
```

回復量はサーバが返した実際の増分を出す。所持上限に達していて何も増えなかった
場合は 0 が返るので、通常の「保存しました」に戻る。

4つとも任意。
変更は公開 Persona プロフィールに即座に反映される。

「公開する」という状態は無い。
B属性がすべて空でも、Persona は生成された時点で公開される。

---

# 16. 主要な画面

MVP の主要画面はちょうど次のとおり:

1. 探す （下部ナビ中央の主 CTA — §14 を参照）
2. Likeされた （タブ）
3. Match （タブ）
4. マイページ （タブ。元のホーム画面を置き換える — §14 を参照）
5. 送信済みLike （タブ）
6. プロフィール編集 （マイページから遷移）

補助的な UI / モーダルの状態:
- 新しい人生を始める
- Persona 開示演出（抽選中の溜め → 1属性ずつの開示 → 総合レア度）
- Match アニメーション（回復した Like をこの中に出す — §8 を参照）
- 23:55 の5分前警告
- 00:00 の終了
- 「この画面では遊べません」（Cookie が保存されない環境の行き止まり — §3 を参照）

> **改訂メモ（実装時に追加）**
> 「Persona 生成アニメーション」を、A属性を1つずつ開示するガチャ演出に
> 置き換えた。演出そのものの仕様は §7.7 を参照。
>
> 演出は**アプリ全体に被せるオーバーレイ**にすること。`POST /api/persona` が
> 返った時点で「当日の Persona が有る」状態に移り、開始画面はアンマウント
> されるため、開始画面の中に演出を置くと結果を出す前に消える。
>
> 人生が始まる入口は「開始画面」と「今日の人生が終了しました」モーダルの
> 2か所ある。どちらから入っても同じ演出に着地させ、演出を閉じたらマイページ
> に降りる。抽選中の溜めと失敗時の再試行もこのオーバーレイが引き受ける。
>
> 進行は自動送りとし、画面のどこをタップしても「即確定 → 次へ」と進める。
> 連打で最後まで飛ばせること。`prefers-reduced-motion` では回転と粒を止め、
> 順に出すだけにする（全属性は変わらず1つずつ出す）。

> **改訂メモ（実装時に追加）**
> ブラウザが Cookie を保存しない場合、リクエストのたびに別人になり
> 何をしても CSRF エラーになる。エラーを繰り返し見せるより、そう分かった
> 時点でアプリ全体を差し替え、最上位のタブで開く導線だけを出す。
> 判定は `GET /api/home` の `cookie_received` で行う。初回は Cookie が無いのが
> 当たり前なので、`false` のときだけもう一度だけ叩き、2回連続で `false` なら
> 保存されていないと見なす。

---

# 17. フロントエンドの状態の扱い

探す画面は5件ずつバッチで取得する。

同一ページセッションのあいだ保持するもの:
- 現在の探すカード
- 取得済みのバッチ
- バッチ内の位置
- ローカルな5枚ぶんの Pass クールダウン除外リスト

探す → Likeされた → 探す と移動した場合:
- 直前のカード / バッチから再開する

ページを完全に再読み込みした場合やブラウザを再起動した場合:
- 探す画面のメモリ上の状態は失われてよい
- 取り直す
- まだ操作していない Persona が再登場してよい
- これは許容する

次のものについてはサーバの状態が正:
- Like
- Pass 回数
- Match
- Persona
- Like 予算

---

# 18. データベーススキーマ

主キーは UUID を使う。

## participants

```sql
CREATE TABLE participants (
    id UUID PRIMARY KEY,
    cookie_token UUID NOT NULL,
    game_date DATE NOT NULL,
    csrf_token TEXT NOT NULL,
    likes_last_seen_at TIMESTAMPTZ NULL,
    matches_last_seen_at TIMESTAMPTZ NULL,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),

    UNIQUE (cookie_token, game_date)
);
```

## personas

```sql
CREATE TABLE personas (
    id UUID PRIMARY KEY,
    participant_id UUID NOT NULL UNIQUE
        REFERENCES participants(id)
        ON DELETE CASCADE,

    age SMALLINT NOT NULL,
    gender TEXT NOT NULL,
    height_cm SMALLINT NOT NULL,
    education TEXT NOT NULL,
    occupation TEXT NOT NULL,
    annual_income INTEGER NOT NULL,

    name VARCHAR(20) NULL,
    hobby VARCHAR(30) NULL,
    bio VARCHAR(60) NULL,

    exposure_count INTEGER NOT NULL DEFAULT 0,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
```

プロフィール写真の追加にあわせて `photo_updated_at TIMESTAMPTZ NULL` を後から
追加した（§5.3 を参照）。写真の実体は DB ではなくファイルとして保存する。

Like の残数と回復の状態も後から追加した（§8 を参照）:

```sql
like_balance SMALLINT NOT NULL DEFAULT 10,
profile_reward_claimed BOOLEAN NOT NULL DEFAULT FALSE,
match_reward_count SMALLINT NOT NULL DEFAULT 0,
like_recovery_anchor_at TIMESTAMPTZ NULL
```

残数は `like_balance` として明示的に持つ。送信済み Like 数からの引き算では
時間回復（Like を送っていないのに残数が増える）を表せない。所持上限は付与の
時点で切り詰めるので、溢れた分が後から復活することはない。

`like_recovery_anchor_at` は次の時間回復を計る起点。初めて Like を消費した
時刻が入り、経過した3時間単位ぶんだけ進む。`NULL` は「まだ Like を使って
いない」を意味し、この状態ではタイマーが動かない。`recovery_started_at` と
`last_like_recovery_at` を別々に持つ必要はない。前者は「NULL かどうか」で
表せるため、2つ持つと同じ意味の状態が重複する。

時間回復の回数は数えない。回数の上限が無く、上限で受け取れなかった分も起点を
進めて捨てるため、「あと何回もらえるか」という状態がどこにも要らない。

これを participants ではなく personas に置いたのは2つの理由から:
- persona 行はゲーム日ごとに作り直されるため、0:00 のリセットがデフォルト値で
  そのまま実現でき、リセット処理を書く必要がない
- Like / Pass のトランザクションはすでに persona 行を `FOR UPDATE` でロックして
  いるため、Match 報酬の更新に新しいロックを足さずに済む

推奨する CHECK 制約:
- age は 20 以上 50 以下
- height_cm は 140 以上 200 以下
- annual_income >= 0
- exposure_count >= 0
- like_balance は 0 以上 12 以下（所持上限）
- match_reward_count は 0 以上 2 以下

## likes

```sql
CREATE TABLE likes (
    id UUID PRIMARY KEY,
    from_persona_id UUID NOT NULL
        REFERENCES personas(id)
        ON DELETE CASCADE,
    to_persona_id UUID NOT NULL
        REFERENCES personas(id)
        ON DELETE CASCADE,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),

    UNIQUE (from_persona_id, to_persona_id),

    CHECK (from_persona_id <> to_persona_id)
);
```

## passes

```sql
CREATE TABLE passes (
    id UUID PRIMARY KEY,
    from_persona_id UUID NOT NULL
        REFERENCES personas(id)
        ON DELETE CASCADE,
    to_persona_id UUID NOT NULL
        REFERENCES personas(id)
        ON DELETE CASCADE,

    pass_count SMALLINT NOT NULL DEFAULT 1,
    last_passed_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),

    UNIQUE (from_persona_id, to_persona_id),

    CHECK (from_persona_id <> to_persona_id),
    CHECK (pass_count >= 1)
);
```

`pass_count` に上限は無い。候補の除外には使わず、Pass の回数をそのまま
記録するだけの列である。

## matches

```sql
CREATE TABLE matches (
    id UUID PRIMARY KEY,

    persona_low_id UUID NOT NULL
        REFERENCES personas(id)
        ON DELETE CASCADE,

    persona_high_id UUID NOT NULL
        REFERENCES personas(id)
        ON DELETE CASCADE,

    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),

    UNIQUE (persona_low_id, persona_high_id),

    CHECK (persona_low_id <> persona_high_id)
);
```

アプリケーション側が INSERT 前に Persona のペアを正規化すること。

---

# 19. 日次の物理削除

正しさは削除のタイミングではなく `game_date` で担保する。

削除ジョブ:
- 定期的に `participants WHERE game_date < 当日(JST)` を削除する
- `ON DELETE CASCADE` により次も削除される:
  - personas
  - likes
  - passes
  - matches
- プロフィール写真は `ON DELETE CASCADE` の対象外なので、`game_date` ごとの
  ディレクトリを削除して掃除する（§5.3 を参照）

ジョブは冪等でリトライ可能であること。

履歴の永続保管はしない。

---

# 20. CSRF / Web セキュリティ

identity が Cookie ベースで、かつ埋め込まれうるアプリなので:

## CSRF

Participant は日次の CSRF トークンを1つ保持する。

生成:
- 暗号論的に安全な乱数
- 32バイト程度
- Base64URL 相当の不透明なトークン

ライフサイクル:
- その日の Participant に対して生成する
- 有効なのはそのゲーム日のあいだだけ
- 初期化 / ホームの取得時に返す
- フロントエンドはメモリ上に保持する
- 更新系リクエストは次のようなヘッダで送る:

```text
X-CSRF-Token: ...
```

CSRF トークンを必須とするもの:
- Persona 生成
- プロフィール更新
- プロフィール写真のアップロードと削除
- Like
- Pass
- 今後追加されるあらゆる更新系エンドポイント

GET のエンドポイントはゲーム状態を変更しないこと。ただし後述の「最終閲覧時刻」の
更新だけは明示的に許容する。

## XSS

B属性はすべて:
- プレーンテキスト
- 描画時にエスケープする
- 生の HTML を挿入しない
- Markdown を解釈しない

## サーバ側の検証

次についてフロントエンドを信用しないこと:
- Like 数
- Like 予算
- Persona の属性
- Match の生成
- Pass 回数
- 文字数制限
- 改行の禁止
- 明示的な URL の禁止
- アップロードされた画像の形式・寸法・容量

---

# 21. API

ベースパス:

```text
/api
```

成功時のペイロードに共通のラッパーは不要。

エラーは次の形式:

```json
{
  "error": {
    "code": "LikeLimitExceeded",
    "message": "今日のLikeを使い切りました"
  }
}
```

フロントエンドは `error.code` で分岐すること。

## ドメインのエラーコード

- `PersonaNotGenerated`
- `LikeLimitExceeded`
- `AlreadyLiked`
- `TargetPersonaUnavailable`
- `SelfActionNotAllowed`
- `DayExpired`
- `InvalidProfileInput`

HTTP との対応:

| エラー | HTTP |
|---|---:|
| InvalidProfileInput | 400 |
| PersonaNotGenerated | 404 |
| TargetPersonaUnavailable | 404 |
| AlreadyLiked | 409 |
| DayExpired | 409 |
| LikeLimitExceeded | 422 |
| SelfActionNotAllowed | 422 |

---

## GET `/api/home`

責務:
- その日の Participant の存在を保証する
- アプリの現在の状態を返す

例:

```json
{
  "server_time": "2026-08-16T21:00:00+09:00",
  "game_date": "2026-08-16",
  "persona_generated": true,
  "persona": {},
  "remaining_likes": 7,
  "like_capacity": 12,
  "received_like_count": 4,
  "match_count": 2,
  "has_unseen_likes": true,
  "has_unseen_matches": false,
  "profile_reward_available": true,
  "next_recovery_at": "2026-08-16T22:10:00+09:00",
  "likes_recovered": 0,
  "csrf_token": "...",
  "cookie_received": true
}
```

`next_recovery_at` は次に時間回復が起きる時刻。タイマーを出す状態でなければ
null（§8 / §14 を参照）。画面はこの null をそのまま「表示しない」条件に使える。
次の回復が 0:00 より後になる場合も null にする。日次リセットとの整合性を
優先するため、決して満了しないタイマーは出さない。

`likes_recovered` はこのリクエストの中で時間回復した数。0 より大きいときだけ
画面が軽い通知を出す。

`like_capacity` は所持上限。残数の分母として画面に出す。値は定数だが、上限を
決めるのはサーバなのでフロントに定数を置かずここから配る。残数を返す他の
エンドポイントも同じ3項目を返す。

Persona が未生成の場合:
- `persona_generated=false`
- `persona` は null でよい

`cookie_received` は、このリクエストが Cookie を伴って届いたかどうか。
初回アクセスでは `false`。2回続けて `false` ならブラウザが Cookie を
保存していないので、フロントエンドは §16 の「遊べません」画面に切り替える。

---

## POST `/api/persona`

CSRF 必須。

冪等:
- その日の Persona が無い → 生成して保存する
- ある → 既存の Persona を返す

振り直しは絶対にしない。

---

## GET `/api/persona/me`

自分の Persona を A属性 + B属性 込みで返す。

---

## PATCH `/api/persona/profile`

CSRF 必須。

リクエスト:

```json
{
  "name": "...",
  "hobby": "...",
  "bio": "..."
}
```

受け付けるのは B属性のみ。

A属性を黙って受け入れないこと。

レスポンス:

```json
{
  "persona": {},
  "remaining_likes": 11,
  "like_capacity": 12,
  "likes_gained": 1,
  "next_recovery_at": null,
  "likes_recovered": 0
}
```

カードをそのまま返さず封筒に入れるのは、プロフィール完成報酬で回復した Like を
同じ応答で伝えるため（§8 を参照）。`likes_gained` は実際に増えた数で、条件を
満たさないときも所持上限で溢れたときも 0 になる。

`profile_reward_available` は「まだ受け取れるか」。保存した直後に訴求を
引っ込めるために返す。`GET /api/home` も同じ項目を返す。

保存・完成条件の判定・報酬の付与・受け取り済みフラグの更新は、同一の
トランザクションで行う。

---

## POST `/api/persona/photo`

CSRF 必須。

ボディは生の画像。サーバが必ずデコードして再エンコードする（§5.3 を参照）。
更新後の Persona カードを返す。

---

## DELETE `/api/persona/photo`

CSRF 必須。

プロフィール写真を削除し、更新後の Persona カードを返す。

---

## GET `/api/personas/{persona_id}/photo`

当日の Persona の写真を配信する。写真が無い場合や当日の Persona でない場合は
`TargetPersonaUnavailable` を返す。

---

## GET `/api/discover`

公開 Persona カードを最大5件返す。

任意のクエリ:

```text
?exclude=id1,id2,id3
```

フロントエンドは現在のクールダウン除外 ID を送ってよい。

このレスポンスで露出を加算しないこと。

カードのほかに `remaining_likes` / `like_capacity` / `next_recovery_at` /
`likes_recovered` も返す。探す画面は長く開かれたままになるので、カードを
継ぎ足すこの往復が時間回復を画面へ反映する機会にもなる（§8 を参照）。

---

## POST `/api/likes`

CSRF 必須。

リクエスト:

```json
{
  "persona_id": "..."
}
```

トランザクション内で:
1. 当日 / 自分の Persona を検証する
2. 対象を検証する
3. 自分自身なら拒否する
4. 重複なら拒否する
5. 時間回復を反映する（残数0のまま開いていた画面から送っても、3時間が
   経っていればその場で1つ送れる）
6. 残数 > 0 を強制する（予算そのものではなく、回復ぶんを含めた残数で見る）
7. Like を INSERT し、残数を1つ減らす。その日の1つ目なら回復の起点を置く
7. 対象の exposure_count を加算する
8. 逆方向の Like を確認する
9. 逆方向が存在すれば、正規化した Match を冪等に作成する
10. Match が新しく成立したなら、ペアの両方に Match 報酬を払う

レスポンス例:

```json
{
  "remaining_likes": 6,
  "like_capacity": 12,
  "matched": true,
  "match_id": "...",
  "likes_gained": 2,
  "next_recovery_at": "2026-08-16T22:10:00+09:00",
  "likes_recovered": 0,
  "target_persona": {}
}
```

`likes_gained` は Match 報酬で実際に増えた数（§8 を参照）。所持上限や回数の
上限で何も増えなかった場合は 0 になり、画面は回復の表示を省く。

Match していない場合:
- `match_id` / `target_persona` は省略または null でよい

---

## POST `/api/passes`

CSRF 必須。

リクエスト:

```json
{
  "persona_id": "..."
}
```

トランザクション内で:
1. 当日 / 対象を検証する
2. 自分自身なら拒否する
3. 対象の状態が不正なら拒否する
4. Pass を INSERT するか加算する
5. 対象の exposure_count を加算する

回数による打ち止めは無いので、Pass が回数を理由に失敗することはない。

レスポンス:

```json
{
  "pass_count": 2
}
```

---

## GET `/api/likes/received`

現在の Persona に Like した Persona を返す。
新しい順。

この画面を開くと Like を既読にするため次を更新する:

```text
participants.likes_last_seen_at
```

MVP にページングは無い。

---

## GET `/api/likes/sent`

現在の Persona が Like した Persona を返す。
新しい順。

各要素に次を追加で含める:

```json
{
  "matched": true
}
```

ページングは無い。

---

## GET `/api/matches`

Match した相手の Persona を返す。
新しい順。

この画面を開くと次を更新する:

```text
participants.matches_last_seen_at
```

ページングは無い。

---

# 22. トランザクションと並行性の重要な要件

## Persona の生成
競合に対して安全であること。

DB の一意制約:
- `(cookie_token, game_date)`
- `personas(participant_id)`

同時にリクエストが来た場合:
- 同じ Persona を返すこと

## Like 予算
サーバ側のトランザクションで強制すること。

2つのタブから操作しても、その日の残数を超えて Like できてはならない。

重複リトライ:
- Like を二重に消費してはならない

## Like の回復
回復量・回数・所持上限のいずれもサーバ側で強制すること。

判定はすべて persona 行のロックの内側で行う。ロックを取ってから状態を読み直す
ことが要点で、トランザクションの外で読んだ persona には古い残数が入っている。

時間回復も同じロックの内側で評価する。次のいずれも起きてはならない:
- 複数タブから同時にアクセスして同じ3時間ぶんが二度付与される
- GET が反映した回復を、続く POST がもう一度適用する
- 所持上限 12 を超える
- 時間回復と Like 消費、あるいは時間回復と Match 報酬が競合して残高が壊れる
- 前日の起点が翌日に適用される（persona 行が日ごとに作り直されるため、
  構造上そうならない）

所持上限で1つも増えなかった場合も起点は進むため、書き込み自体は起きる。
この更新もロックの内側で行うので、同じ3時間が二度消費されることはない。

- プロフィール完成報酬は、保存・判定・付与・フラグ更新を同一トランザクションに
  入れる。同じ PATCH が同時に2つ届いても、後から入った方はフラグが立った後の
  行を読むため、報酬は1日1回で止まる
- Match 報酬は Like のトランザクションに相乗りする。ペアの両行をすでに
  `FOR UPDATE` しているので、同時 Like でも回数が2を超えない
- 同一の Match に二重付与しないことを、Match の存在確認で明示的に否定する

同時に複数の Match が成立しても、回復量の合計が `+2 × 2` を超えてはならない。

## Match
冪等であること。

ID を正規化する:

```text
low_id = min(personaA, personaB)
high_id = max(personaA, personaB)
```

DB の一意制約が Match の重複を防ぐ。

---

# 23. MVP で作らないもの

コアの体験を成立させるために必要でない限り、次は実装しない:

- チャット
- DM
- ブロック
- 通報
- モデレーション画面
- プッシュ通知
- ブラウザ通知
- WebSocket
- SSE
- スワイプ操作
- 履歴
- 累積統計
- ランキング
- Like 率
- ユーザーに見せる露出統計
- 保存される / 比較されるレア度・スコア（→ 改訂メモ）
- 検索
- 性別フィルタ
- マッチング条件
- ページング
- ソーシャルログイン
- kusa 連携 API
- 再抽選対策 / Cookie 削除対策
- 高度な不正対策
- NG ワード辞書
- 電話番号 / メール / SNS ID の検出
- 長期プロフィール
- 顔の魅力度スコア
- 「親の資産」のような追加ステータス

> **改訂メモ（実装時に追加）**
> 「レア度 / SSR / スコア」は当初まるごと作らない対象だったが、開示演出の中
> だけで使う表示専用のレア度を入れた（§7.7）。作らないままなのは次のとおり:
> DB に保存するレア度、他人と比較できるレア度、レア度によるランキング、
> レア度がマッチングや露出に影響する仕組み。
> レア度は演出が終われば消え、どこにも残らない。

---

# 24. 完成の定義

次の一連の流れが通ればこの MVP は完成:

```text
アプリを開く
  ↓
匿名 Cookie を発行 / 読み取り
  ↓
その日の Participant を作成
  ↓
「新しい人生を始める」
  ↓
サーバがランダムな Persona を1つ生成
  ↓
A属性を1つずつ開示し、総合レア度を出す
  ↓
Persona が即座に市場へ参加
  ↓
任意で 名前 / 趣味 / ひとこと / 写真 を編集
  ↓
探す画面を開く
  ↓
候補を5件取得
  ↓
1枚ずつ Persona を表示
  ↓
Like / Pass
  ↓
10 Like の予算が強制される
  ↓
Likeされた を閲覧できる
  ↓
Like を返せる
  ↓
相互 Like で Match が成立
  ↓
Match アニメーションが出る
  ↓
送信済みLike と Match を確認できる
  ↓
マイページにカウンタ、ヘッダーに日次カウントダウン
  ↓
23:55 の警告
  ↓
00:00 に前日の状態が無効になる
  ↓
「今日の人生が終了しました」
  ↓
翌日の「新しい人生を始める」
```

これが安定して動くなら、機能追加をやめて実ユーザーで試すこと。

---

# 25. プロジェクト構成の案

名前は調整してよいが、責務は明示的に保つこと。

```text
.
├── cmd/
│   └── server/
│       └── main.go
├── internal/
│   ├── participant/
│   ├── persona/
│   ├── matching/
│   ├── photo/
│   ├── http/
│   │   ├── handler/
│   │   ├── middleware/
│   │   └── response/
│   ├── db/
│   │   ├── query/
│   │   └── sqlc/
│   └── clock/
├── migrations/
├── web/
│   ├── src/
│   │   ├── lib/
│   │   ├── components/
│   │   ├── routes/
│   │   └── stores/
│   └── vite.config.*
├── sqlc.yaml
├── go.mod
└── README.md
```

実際の構成は README を参照。

アーキテクチャ上の潔癖さのためだけに、repository / service / usecase の層を
作り込まないこと。次のものが明確でテスト可能である程度の分離にとどめる:
- HTTP
- ドメインのルール
- SQL
- フロントエンド

---

# 26. 実装の推奨順

1. Go + chi + PostgreSQL + sqlc + マイグレーションの足場
2. JST のゲーム日付クロックの抽象化
3. Cookie による participant identity
4. Participant の ensure ロジック
5. マイグレーションとスキーマの作成
6. Persona 生成器 + ユニットテスト
7. Persona 生成エンドポイント
8. ホームのエンドポイント
9. プロフィール編集の検証
10. 探すクエリ
11. Pass のトランザクション
12. Like のトランザクション + 10件上限
13. Match の生成
14. 一覧 API
15. CSRF
16. Svelte のシェル / マイページ
17. Persona カード
18. 新しい人生を始めるフロー
19. 探すフロー + 5枚の先読み
20. Likeされた / 送信済みLike / Match の各画面
21. Match アニメーション
22. 日次カウントダウン + 23:55 + 00:00 の遷移
23. 物理削除ジョブ
24. iframe 内の動作確認
25. 直接 URL での動作確認
26. Persona 生成と10 Like予算まわりの並行性テスト

---

# 27. 重要なテスト

次を優先する。

## Persona 生成
- 年齢が常に 20〜50
- 身長が常に 140〜200
- 大卒が22歳未満にならない
- 大学院卒が24歳未満にならない
- ホイ卒を除き医師の制約が守られる
- ホイ卒を除き教員の制約が守られる
- ホイ卒を除き経営者の年齢制約が守られる
- 年収が常に職業のレンジ内
- 年収が常に10の倍数

## Like
- 自分に Like できない
- 同じ相手に重複して Like できない
- 10件の Like が成功する
- 11件目の Like が失敗する
- 同時に Like しても10件を超えない
- リトライで二重に消費しない
- 相互 Like で Match が1件だけできる

## Like の回復
プロフィール完成報酬:
- 3項目すべて設定で +1
- 一部未入力では発生しない
- 同じ日に2回発生しない
- 一度完成させた後に消して入れ直しても再発しない
- 所持上限 12 を超えない

Match 報酬:
- 1回目の Match で +2
- 2回目の Match で +2
- 3回目以降は回復しない
- 同一の Match で二重付与されない
- 同時に Match しても回数の上限を超えない
- 所持上限 12 を超えない
- ペアの両方が受け取る

時間回復:
- Like を1つも使っていなければタイマーが始まらない
- 初めての Like 消費でタイマーが始まる
- 2つ目以降の Like で起点が今に進まない
- 3時間未満では回復しない
- 3時間で +1
- 6時間で +2
- 9時間で +3。回数の上限は無い
- アクセスしていない時間も計算対象。9時間30分ぶりの再訪で +3、余った30分は
  次回へ持ち越される
- 所持上限 12 を超えない
- 11 に3回ぶんが成立しても +1 で止まり、残り2回ぶんは失われる。後から Like を
  使ってもその2回ぶんは戻らない
- 12 / 12 のまま経過した3時間はストックされない。空きができた後は通常の間隔から
  回復し直す
- 複数タブから同時にアクセスしても同じ3時間ぶんが二重に付与されない
- GET で反映した回復を、続く POST が再適用しない
- 残数0でも、3時間が経っていれば Like の POST がその場で通る
- `GET /api/discover` でも回復が反映される

回復ぶんが実際に使えること:
- 報酬だけで満額まで回復した日は Like を15回送れる
- 時間回復も足せば、その日に残っていた時間ぶんだけ伸びる

訴求（`profile_reward_available`）:
- 未取得なら true
- 一部だけ埋めた状態でも true
- 完成させた保存の応答で false になる
- 項目を消しても true に戻らない
- 翌日は true に戻る

日付境界:
- 翌日はプロフィール報酬をもう一度受け取れる
- 翌日は Match 報酬の回数が 0 に戻る
- 翌日は時間回復の起点が初期化される。23:59 の回復状態は引き継がない
- 前日の起点を翌日に使わない。新しいタイマーはその日の初 Like から始まる

## Pass
- pass_count が 1 → 2 → 3 … と増え、上限で止まらない
- 何度 Pass した相手も探すの候補に残る
- 成功した操作1回につき exposure が1増える

## 日付境界
- 前日の Persona は当日に行動できない
- 前日の Like / Match が当日に現れない
- 同じ Cookie で翌日の Participant が作れる
- 翌日の Persona が生成できる
- 前日の Persona は再利用されない

## セキュリティ
- 更新系で不正な CSRF が拒否される
- プロフィールの PATCH で A属性を変更できない
- 改行が拒否される
- 明示的な URL が拒否される
- HTML がテキストとして扱われる

## プロフィール写真
- 画像でないアップロードが拒否される
- 展開爆弾がデコード前に拒否される
- 再エンコードで EXIF が消える
- 保存される画像が長辺1024px以下になる
- 前日ぶんの写真が日次クリーンアップで消える

---

# 28. 最後に

コアとなる1日のループを中心に MVP を実装すること。

指定が無い箇所では:
1. 最も単純な実装を選ぶ
2. 日次の一時性を守る
3. Like が希少資源であることを守る（1日10 + 少量の回復、所持上限 12。§8 を参照）
4. Persona と User の分離を守る
5. 機能を足さない
6. 現在の MVP が正当化しないアーキテクチャを持ち込まない

実装を進める中でこの仕様に本当の矛盾が見つかった場合は、勝手に新しいプロダクト
ルールを作らず、その矛盾を明示して止まること。
